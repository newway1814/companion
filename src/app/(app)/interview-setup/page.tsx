export default function InterviewSetupPage() {
  return (
    <div className="mx-auto max-w-3xl p-gutter">
      <h1 className="font-heading text-display-md text-on-surface">
        Interview setup
      </h1>
      <p className="mt-2 max-w-prose text-body-lg text-on-surface-variant">
        The pre-session confirmation — source profile, target position, detected
        context, and the five-question framing — is coming next.
      </p>
    </div>
  );
}
